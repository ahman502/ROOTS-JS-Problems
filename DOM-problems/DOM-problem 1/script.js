function changeBackgroundColor() {                                    // function that is called when the button in clicked
    document.getElementById("p1").style.backgroundColor = "red";      // I get the element in the HTML document that has the id of p1 (paragraph element)
  }                                                                   // And, I set it's background color's style to red